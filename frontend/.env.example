# Copy to .env.local for dev. In Netlify, set these as environment variables.
VITE_API_BASE=http://localhost:8000
# Must match the backend REVIEWER_TOKEN (leave blank if the backend has none).
VITE_REVIEWER_TOKEN=
